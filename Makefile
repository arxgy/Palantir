# TODO: macro, subdir
CC	= $(MULTILIB_TOOLCHAIN)riscv64-unknown-linux-gnu-gcc
LINK	= $(MULTILIB_TOOLCHAIN)riscv64-unknown-linux-gnu-ld
AR	= $(MULTILIB_TOOLCHAIN)riscv64-unknown-elf-ar
all:
	MULTILIB_TOOLCHAIN=$(MULTILIB_TOOLCHAIN) CC=$(CC) LINK=$(LINK) AR=$(AR) make -C tools
	MULTILIB_TOOLCHAIN=$(MULTILIB_TOOLCHAIN) CC=$(CC) LINK=$(LINK) AR=$(AR) make -C lib
	MULTILIB_TOOLCHAIN=$(MULTILIB_TOOLCHAIN) CC=$(CC) LINK=$(LINK) AR=$(AR) make -C demo


clean:
	MULTILIB_TOOLCHAIN=$(MULTILIB_TOOLCHAIN) CC=$(CC) LINK=$(LINK) AR=$(AR) make -C tools clean
	MULTILIB_TOOLCHAIN=$(MULTILIB_TOOLCHAIN) CC=$(CC) LINK=$(LINK) AR=$(AR) make -C lib clean
	MULTILIB_TOOLCHAIN=$(MULTILIB_TOOLCHAIN) CC=$(CC) LINK=$(LINK) AR=$(AR) make -C demo clean
